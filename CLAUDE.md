# CLAUDE.md — Duckov_Like

이 문서는 **이 프로젝트 고유의 규칙**만 담는다. 응답 스타일·작업 원칙 같은 메타 규칙은
user-level `~/.claude/CLAUDE.md`에 있고 자동으로 함께 로드된다. 여기에 반복하지 않는다.

> **user-level 규칙을 덮는 항목**: user-level CLAUDE.md는 코드 주석을 영어 기본으로 정하지만,
> **이 프로젝트에서는 주석도 한글로 쓴다**(4절). 커밋 메시지 형식은 user-level과 동일하다.
> 충돌 시 프로젝트 규칙이 우선한다.

---

## 1. 프로젝트

싱글플레이 탑다운 익스트랙션 슈터. **포트폴리오 프로젝트이며 상용 게임 완성이 목적이 아니다.**

핵심 명제: **CommonUI·UMG 기반 UI를 구현하고, MVVM과 그리드 인벤토리 Model의 연결을
동작·검증·구조 설명으로 보여주는 포트폴리오를 완성한다.**

- 엔진: **UE 5.7** (`C:\Program Files\Epic Games\UE_5.7`) · 플랫폼: Windows(Desktop)
- 레퍼런스: Escape From Duckov (Team Soda) — 설계 분석 대상이지 클론 대상이 아니다
- 기술 증명 축 우선순위: **①CommonUI·UMG 사용자 경험 ②MVVM과 검증 가능한 인벤토리 Model ③GAS**.
  리소스가 부족하면 ③부터 줄인다. CommonUI는 목표이며 현재 활성화·구현된 상태를 뜻하지 않는다.

판단이 갈릴 때의 기준: **UI 경험·구조·검증을 보여주는 데 필요한 기능을 우선하고,
이를 뒷받침하지 않는 게임 시스템 확장은 v1에서 뺀다.** 기존 마일스톤 순서는 별도 범위 합의 없이 바꾸지 않는다.

## 2. 모듈 경계 — 이 프로젝트에서 가장 중요한 규칙

| 모듈 | 책임 | 의존 |
| --- | --- | --- |
| `InventoryCore` | 순수 Model — 그리드, 배치 규칙, 연산 서비스, 저장 레코드 | `Core`, `CoreUObject`, `Engine` |
| `DuckovLike` | 게임 모듈 — ViewModel, 위젯, 액터, GAS | 위 + `InputCore`, `EnhancedInput`, `InventoryCore`, `UMG`, `ModelViewViewModel` |

- **`InventoryCore`의 `Build.cs`에 `UMG`/`Slate`/`SlateCore`/`ModelViewViewModel`을 절대 넣지 않는다.**
  이걸 넣는 순간 이 프로젝트의 핵심 논증이 무너진다. 필요해 보이면 설계가 잘못된 것이다.
- 의존 방향은 `View -> ViewModel -> Model` 단방향. 역방향은 이벤트(ChangeSet)로만.
- `InventoryCore`는 `Public/`·`Private/` 분리. **다른 모듈에 노출할 타입만 `Public/`.**
  `DuckovLike`는 소비자가 없으므로 평면 구조.

경계가 실제로 작동하는 지점(M0.5 실측): UMG 헤더 `#include`와 타입 선언은 **통과한다**
(에디터 타깃의 `SharedPCH.UnrealEd`가 이미 담고 있다). `UUserWidget::StaticClass()`처럼
**심볼을 참조하면 `LNK2019`로 링크가 깨진다.** 즉시성은 없지만 우회는 불가능하다.

## 3. 커밋 규칙

- **논리 단위로 쪼갠다.** 한 커밋은 한 가지 일만 한다. "겸사겸사" 변경을 섞지 않는다.
- **각 커밋은 독립적으로 빌드되어야 한다.** 커밋 전에 실제로 빌드를 돌려 확인한다.
- **빌드가 깨진 상태로 커밋하지 않는다.**

형식:

```
<type>(<scope>): <제목: 한글, 50자 내외, 마침표 없음>

<본문: 한글. 무엇이 아니라 "왜". 버린 대안과 판단 근거를 남긴다.
 diff를 읽으면 아는 내용을 반복하지 않는다.>

검증: <검증 명령> -> <결과>

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

- **제목과 본문은 한글로 쓴다.** `type`/`scope`와 `Co-Authored-By` trailer는 영어 그대로 둔다.
- 기술 용어는 번역하지 않는다 — "그리드 인벤토리의 widget hierarchy 최적화" 같은 형태.
- 제목은 "~ 추가 / ~ 수정 / ~ 제거"처럼 끝낸다. 영어의 명령형을 억지로 옮기지 않는다.

**type** (필수):

| type | 용도 |
| --- | --- |
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 동작이 바뀌지 않는 구조 변경 |
| `test` | Automation Test 추가·수정 |
| `build` | 모듈 구성, `Build.cs`, 플러그인, `.uproject`, 빌드 설정 |
| `docs` | 문서, 설계결정기록 |
| `perf` | 성능 개선 (측정 결과를 본문에 남긴다) |
| `chore` | 그 외 — 설정 파일, 정리 |

**scope** (선택): 모듈이나 영역. `inventory`, `ui`, `gas`, `lfs` 등.
애매하면 생략한다 — 억지로 붙이지 않는다.

예시:

```
build: InventoryCore 모듈 추가 — UI 비의존 경계를 빌드 시스템으로 강제
feat(inventory): row-major first-fit 퀵 이동 구현
test(inventory): 컨테이너 간 이동의 실패 원자성 테스트 추가
docs: 모듈 경계 결정을 설계결정기록 000에 기록
```

- `검증:` 줄은 빌드·테스트를 돌린 커밋에 넣는다. 돌리지 않았으면 넣지 않는다 — 거짓 신호 금지.

> commit·push 권한과 branch 운용은 user-level `~/.claude/CLAUDE.md`가 정한다.
> 여기에 반복하지 않는다.

## 4. 코딩 규칙

UE 표준 코딩 규약을 따른다. 아래는 이 프로젝트에서 추가로 지킬 것.

- **한글로 쓰는 것**: 주석, 커밋 메시지, 문서, 로그 메시지 등 사람이 읽는 모든 텍스트.
  **영어로 쓰는 것**: 식별자(변수·함수·클래스·모듈·파일명)와 기술 용어.
  → "`InventoryContainer`의 점유 cache를 재생성한다" 처럼 섞어 쓴다. 기술 용어를 번역하지 않는다.
- **최소 코드.** 추측에 기반한 유연성·확장성·에러 처리를 넣지 않는다.
  빈 클래스 스텁을 미리 만들어 두지 않는다.
- **실패는 명시적으로.** 인벤토리 연산은 bool이 아니라 실패 사유를 반환한다
  (`NoSpace`, `Occupied`, `InvalidCategory`, `StackFull` …). UI가 이유를 표시할 수 있어야 한다.
- **실패한 연산은 상태를 바꾸지 않는다.** 전체 검증 후 한 번에 커밋한다. 롤백 경로를 만들지 않는다.
- **Model이 진실이다.** 위치·회전·수량은 `InventoryCore`만 소유한다.
  View/ViewModel은 원본 상태를 갖지 않는다.
- **인벤토리 UI는 상시 Tick에 의존하지 않는다.** 이벤트 기반으로 갱신한다.
- 네이밍 세부는 `docs/CONVENTIONS.md`.

## 5. 작업 분담 — 기능 단위 자율 실행과 결과 검토

**기본 흐름은 목표 확인 → AI의 설계·구현·검증 → 사용자의 결과·구조 검토다.**
사용자는 목표·범위·완료 기준과 결과 수용을 맡고, AI는 그 범위 안의 기술 선택과 실행을 맡는다.
2026-09-10부터 기존의 문서별 인터뷰, 사용자 선답변, 헤더·테스트 이름별 승인 절차를 대체한다.
이전 ADR·worklog의 판단 출처와 당시 학습 기록은 보존하되, 거기에 남은 옛 작업 절차를 새 작업의 gate로 재적용하지 않는다.
이 절은 이 프로젝트의 user-level 학습·확인 절차보다 우선한다. Git 권한과 HIGH 변경의 사람 검토는 유지한다.

> **과거 판단 출처 보존(2026-08-09 기록).** 설계결정기록 009 이전의 GDD·INVENTORY_DESIGN·
> 설계결정기록 000·002는 AI가 초안과 근거를 제시하고 사용자가 읽고 납득한 산출물이며,
> 사용자가 스스로 선택지를 훑고 내린 판단은 아니다. 당시 설명 역전에서 설계결정기록 002에 대해
> 재생한 내용은 `struct`, `FGuid` 두 결론 명사였고 근거·정책·대가는 설명하지 못했다
> (`docs/worklog/M0-design.md`). 식별자는 이후 설계결정기록 009에서 재결정했다.
> 이 기록은 당시 이해 상태의 근거이며 현재 이해 상태를 추정하거나 새 작업을 차단하는 데 쓰지 않는다.

### 5.1 작업 단위와 진행 방식

- 문서 한 장이나 함수 하나가 아니라 **관찰 가능한 동작 하나**를 작업 단위로 삼는다.
  예: 인벤토리 열기·닫기와 focus 복귀, 이동 실패 이유 표시.
- 시작 시 목표, 변경 범위, 완료 기준, risk tier(LOW/HIGH)를 짧게 알린다.
  이미 실행을 요청받은 LOW 작업은 계획 승인이나 "진행할까요?"를 다시 묻지 않고 이어간다.
- AI는 기존 코드·Accepted ADR·엔진 제약을 확인하고, 권장안과 중요한 대안 하나의 비용을 설명한 뒤
  LOW 범위에서는 선택·구현·검증까지 수행한다. 모든 선택지를 사용자에게 먼저 작성하게 하지 않는다.
- 정보가 부족해도 되돌릴 수 있는 구현 세부는 가정을 밝히고 진행한다.
  답에 따라 목표·사용자 경험·호환성이 달라지는 질문만 관련 내용을 묶어 한 번에 묻는다.
- 현재 요청 범위가 끝나면 결과를 보고한다. 다음 기능이나 마일스톤까지 임의로 확대하지 않는다.
- 일반 작업에는 HANDOFF/RESULT를 새로 만들지 않는다. 명시적으로 Two-CLI 역할을 사용하거나
  dispatch한 경우에만 해당 protocol을 따른다. 기존 HANDOFF가 있다는 이유만으로 그 작업을 재실행하지 않는다.

### 5.2 자율 실행 범위와 확인 지점

| 구분 | 처리 |
| --- | --- |
| LOW: 요청 범위 안의 새 API·header·specifier·테스트·private 구현, 기존 계약을 보존하는 수정 | AI가 설계부터 검증까지 수행하고 결과에서 핵심 선택을 보고 |
| HIGH: Accepted ADR와 충돌, 기존 API/Blueprint 호출부를 깨는 변경, 저장 포맷·migration, 데이터 삭제, 보안·replication | 충돌·영향·권장안·기존 계약 유지 대안을 한 번에 제시하고 영향받는 구현 전 사용자 확인 |
| 요청 밖의 기능·마일스톤, 포트폴리오 목표나 주요 UX의 변경 | 범위 변경 이유와 최소 대안을 제시하고 확인 |
| commit·push·merge·branch 변경 | user-level Git 권한 준수. 구현 요청만으로 권한이 생기지 않음 |

- 파일이 Public/에 있거나 UPROPERTY/UFUNCTION을 쓴다는 사실만으로 HIGH로 분류하지 않는다.
  기존 호출부·Blueprint·저장 데이터·수명과 소유권에 미치는 실제 영향으로 판단한다.
- Accepted ADR는 계속 Architecture Constraint다. AI가 구현 편의를 위해 몰래 고치거나
  Proposed 문서를 Accepted인 것처럼 취급하지 않는다. 현재 요청에서 이미 승인한 변경은 재확인을 요구하지 않는다.
- 외부 환경 문제나 검증 실패는 원인을 확인해 범위 안에서 수정·재검증한다.
  해결할 수 없으면 확인하지 못한 내용과 필요한 사용자 조치만 구체적으로 보고한다.

### 5.3 헤더와 구현은 AI가 함께 작성한다

- Public/의 타입·시그니처·enum·실패 사유, UPROPERTY/UFUNCTION specifier, .cpp와 private 헬퍼를
  하나의 계약으로 설계하고 구현한다. 기존 계약을 깨는 변경에는 위 확인 규칙을 적용한다.
- 완료 보고에 책임·소유권·주요 public API와 의미 있는 specifier 선택 이유를 짧게 남긴다.
  문법과 boilerplate마다 사용자의 선택이나 설명을 요구하지 않는다.
- Model 상태 소유, 실패 원자성, UI 비의존 경계와 이벤트 기반 갱신 원칙은 유지한다.

### 5.4 테스트 설계와 실행도 AI가 맡는다

- AI가 완료 기준에서 성공·실패·경계 조건을 뽑아 테스트 이름과 검증 내용을 정하고 구현한다.
  사용자에게 테스트 이름 작성을 숙제로 요구하지 않는다.
- Model 계약 변경은 관련 Automation Test로 검증한다. 테스트 수를 채우기 위한 중복 테스트는 만들지 않는다.
- UI 기능은 해당 기능의 화면 동작·입력 전환·focus·취소/복귀·실패 표시 등 필요한 시나리오로 검증한다.
  실제 Editor에서 확인하지 못했다면 자동 검증 결과와 수동 확인 항목을 구분한다.

### 5.5 테스트 모듈 경계

InventoryCoreTests는 기존의 별도 Editor 모듈로 유지하며 InventoryCore의 Public 계약을 테스트한다.
private 구현을 테스트하려고 Public으로 올리지 않는다. 이미 존재하는 모듈의 신설을 다시 계획하지 않는다.

### 5.6 이해 확인은 결과 검토에 붙인다

- 완료 보고는 **동작 변화 / 핵심 책임과 호출·데이터 흐름 / 선택 이유와 버린 대안 하나 /
  실제 검증 결과와 미검증 항목 / 직접 볼 파일 최대 3개**로 간결하게 정리한다.
- 사용자가 판단할 핵심 지점은 1~3개로 압축한다. 모든 파일을 정독하거나 답변해야 다음 작업을 할 수 있게 만들지 않는다.
- 설명 역전·퀴즈·Option Sweep·/walkthrough는 사용자가 학습을 요청할 때만 수행하며 구현 완료 gate가 아니다.
- 구현·검증 상태와 사용자 검토·이해 상태를 분리해 기록한다.
  실제 완료 기준을 충족하면 기술적 완료로 기록하고, 사용자 수용이나 이해를 확인하지 않았다면 그대로 미확인으로 남긴다.
  설명 역전 미실시만으로 기술적 완료를 막거나, 기술적 완료만으로 사용자가 이해했다고 주장하지 않는다.
- "학습 모드"를 요청받으면 그 작업에만 질문 중심 방식을 적용한다. 기본 자율 실행 정책을 영구 변경하지 않는다.

## 6. 빌드와 검증

```
& "C:\Program Files\Epic Games\UE_5.7\Engine\Build\BatchFiles\Build.bat" DuckovLikeEditor Win64 Development "-Project=$PWD\DuckovLike.uproject" -WaitMutex -NoHotReload
```

- **PowerShell 기준이다.** cmd에서는 `-Project="%CD%\DuckovLike.uproject"`로 바꾼다.
  두 형태 모두 UE 5.7에서 동작 확인됐다.
- **`-Project`에 특정 checkout 경로를 하드코딩하지 않는다.** 실제 작업 repo의 CWD에서 `$PWD`로
  절대 경로를 전개해 자기 변경을 빌드한다. 별도 worktree 생성이나 branch 전환을 기본 동작으로 가정하지 않는다.
- **상대 경로도 쓸 수 없다.** `Build.bat`은 UBT를 부르기 전에 `pushd`로 CWD를 엔진의 `Engine/Source`로
  바꾸므로 `-Project=".\DuckovLike.uproject"`는 `Unable to find project file`로 실패한다.
- 첫 빌드는 2분 이상 걸린다. 증분은 10초 내외.
- **`exit 0`을 성공으로 믿지 않는다.** 출력의 `Result: Succeeded`를 확인한다.
- headless 에디터 부팅으로 모듈 로드를 확인할 수 있다. 단 `-ExecCmds="Quit"`은
  **unattended 에디터를 종료시키지 못한다** — 프로세스가 무한히 남으니 쓰지 않거나 반드시 정리한다.
- Model 규칙은 Automation Test로 검증한다. **v1 완료 기준은 30개 이상 통과.**

## 7. 문서

| 문서 | 성격 |
| --- | --- |
| docs/GDD.md | 목표·범위·마일스톤·검증 계획 |
| docs/INVENTORY_DESIGN.md | 불변식, 연산 계약, MVVM 흐름과 초기 제안 |
| docs/CONVENTIONS.md | 네이밍, Content 폴더 규약 |
| docs/architecture/*.md | 구조 결정. TEMPLATE.md는 템플릿, MODULE_MAP.md는 참고 문서 |
| docs/worklog/M*.md | 마일스톤별 구현·검증·판단 출처 기록 |

- 작업 절차는 이 문서의 "작업 분담"을 따른다. GDD·INVENTORY_DESIGN·과거 ADR/worklog의
  일괄 사전 승인이나 설명 역전 요구는 현재 작업을 멈추는 gate가 아니다.
  기술 계약은 해당 Accepted ADR를 우선 확인한다.
- **GDD·INVENTORY_DESIGN은 읽기 전용이다.** 문제를 발견하면 수정하지 않고 보고한다.
  옛 협업 절차의 우선순위만 이 문서로 대체한다. 이번 정책 변경은 기존 기술 계약의 일괄 승인이 아니다.
- 문서를 인용할 때 번호보다 제목을 사용한다. 기존의 깨진 이미지 링크는 별도 작업 범위다.
- **ADR는 새 시스템·모듈 경계·데이터 흐름·패턴 선택 등 오래 남겨야 할 구조 결정에만 작성한다.**
  함수·specifier·테스트 하나마다 만들지 않는다. 관련 결정은 하나의 ADR에 묶는다.
- 새 ADR는 TEMPLATE.md를 사용해 AI가 맥락·선택·근거·대안·비용·재검토 조건·검증을 작성한다.
  LOW 결정은 위임 범위 안에서 Accepted로 기록할 수 있으나 **판단 주체: AI(위임 범위)**를 명시한다.
  사용자 승인이 필요한 결정은 승인 전까지 Proposed다. 사용자가 하지 않은 판단·발언·승인을 만들지 않는다.
- 기존 ADR의 상태는 개별 문서에서 확인한다. 오래된 일괄 상태표를 믿거나 과거 ADR를 자동 승인하지 않는다.
  과거 결정의 출처를 새 방식으로 소급해서 바꾸지 않는다.
- worklog는 **기능 단위 작업이 끝날 때 해당 마일스톤 문서에 한 번 요약**한다.
  별도 문서 작성·승인 대화를 반복하지 않는다.
- 기록은 변경 요약, 실제 검증 결과, 사용자 지시/AI 판단/사용자 수정의 출처, 미결 사항만 간결하게 남긴다.
  커밋 전에는 해시 대신 미커밋 상태를 기록한다. 사용자 원문 인용은 실제 판단 근거가 필요할 때만 한다.
- 실패 기록은 보존한다. 과거 테스트 결과를 이번 실행으로 쓰지 않고, 미실행·시간 초과를 성공으로 기록하지 않는다.
  구현·검증 완료와 사용자 검토 상태를 구분한다.

## 8. 마일스톤과 스코프

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| M0 | 설계 | 초기 문서 작성됨 · ADR별 상태 확인 |
| M0.5 | UE 프로젝트 스캐폴딩, 모듈 분리 | 완료 |
| **M1a** | **Model 핵심 — 배치·이동·스택** | 코드 구현됨 · worklog에 24개 통과 기록 · 사용자 검토 별도 |
| M1b | Model 파생 — 정렬·저장·리사이즈 | |
| M2 | ViewModel + CommonUI·UMG | |
| M3 | 게임 루프 — 루팅·장비·탈출·스태시, GAS | |
| M4 | 성능 점검, 문서·영상 정리 | |

**M1 분할 유지**: 배치·이동·스택과 정렬·저장·리사이즈를 독립적으로 구현·검증한다.
당초에는 헤더를 한꺼번에 확정하는 부담을 줄이고 중간 설명 역전으로 오해를 확인하려고 나눴다.
분할과 순서는 유지하되, 이제 설명 역전이나 개별 헤더 승인은 단계 진입 조건이 아니다.

의존 관계상 이음매는 여기다 — `배치 → 이동 → 스택`이 핵심 사슬이고,
`정렬`·`리사이즈`는 배치를 재사용하며, `저장`은 상태 스냅샷이라 직교한다.

| | 범주 | 대략 테스트 수 |
| --- | --- | --- |
| **M1a** | 배치(경계·겹침·회전·1x1·전체 점유), 이동(동일·타 컨테이너·실패 원자성), 스택(완전/부분 병합·최대치·상태 불일치) | ~18 |
| **M1b** | 정렬(결정론·원상 보존·회전 정책), 저장(포맷 버전·Definition 참조·InstanceId·동일성), 리사이즈(축소 거부·확대) | ~12 |

**현재 마일스톤 밖의 일을 하지 않는다.** M1에서 위젯을 만들지 않고, M1a에서 저장 로직을
쓰지 않는다. 필요해 보이면 먼저 말한다.

의도적으로 제외된 것: **네트워크/Replication**, 베이스 건설, 펫/동료, 스킬 트리, 다수의 맵·적·무기.

## 9. 다음 작업의 판단 기준

- 다음 기능에 필요한 결정만 검토한다. 과거 ADR를 전부 재작성·재승인하는 단계로 돌아가지 않는다.
- 저장·리사이즈는 M1b, ViewModel·ChangeSet·CommonUI·UMG는 해당 UI 기능을 구현할 때 구체화한다.
  단계 순서 변경은 사용자와 범위를 정한 뒤 반영한다.
- CommonUI는 현재 .uproject에 활성화되어 있지 않다. 이후 UI 작업에서 플러그인·모듈 의존과
  입력/focus 흐름을 함께 확인한다. 이 지침의 목표를 이미 구현된 기능으로 보고하지 않는다.
- 현재 상태는 실제 Source와 최신 worklog를 함께 확인한다. M1a는 배치·이동·스택 코드와
  InventoryCoreTests 모듈이 있으며, worklog에는 테스트 24개 통과가 기록돼 있다.
  이 정책 변경에서 테스트를 다시 실행한 것은 아니다.
- 과거 worklog에 남은 설명 역전 대기는 기술 작업의 차단 사유가 아니다.
  사용자 검토를 완료했다고 소급 기록하지 않는다.
